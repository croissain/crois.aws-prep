import { ArrowRight, Check, Clock3, HelpCircle, ListChecks, ShieldCheck } from "lucide-react";
import { Exam } from "@/lib/types";
export function ExamSetup({ exam, reveal, setReveal, start }: { exam: Exam; reveal: boolean; setReveal: (v:boolean)=>void; start:()=>void }) {
  return <section className="mx-auto max-w-4xl px-5 pb-24 pt-12 lg:px-8 lg:pt-16"><div className="text-center"><span className="text-xs tracking-[.18em] text-ai dark:text-[#60a5fa]">MOCK EXAM</span><h1 className="mt-3 text-4xl font-medium tracking-tight">Mock exam</h1><p className="mx-auto mt-4 max-w-lg text-sm leading-7 text-black/50 dark:text-white/50">Measure your current level under realistic exam conditions.</p></div><div className="mt-10 overflow-hidden rounded-[2rem] border border-black/10 bg-white/60 dark:border-white/10 dark:bg-white/[.035]"><div className="grid grid-cols-3 divide-x divide-black/10 border-b border-black/10 p-6 text-center dark:divide-white/10 dark:border-white/10"><Stat icon={<ListChecks/>} value="65" label="questions"/><Stat icon={<Clock3/>} value="130" label="minutes"/><Stat icon={<ShieldCheck/>} value="72%" label="pass target"/></div><div className="p-6 sm:p-9"><h2 className="text-lg font-medium">Choose an exam mode</h2><p className="mt-1 text-xs text-black/45 dark:text-white/45">Choose when answers are revealed during the exam.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><Mode selected={!reveal} onClick={()=>setReveal(false)} title="Exam mode" text="Answers stay hidden until grading"/><Mode selected={reveal} onClick={()=>setReveal(true)} title="Learning mode" text="Check the answer after each response"/></div><div className="mt-8 rounded-xl bg-black/[.035] p-4 text-xs leading-6 text-black/55 dark:bg-white/[.045] dark:text-white/55"><div className="flex gap-3"><HelpCircle size={17} className="mt-0.5 shrink-0 text-ai dark:text-[#60a5fa]"/><p>If fewer than 65 questions are available, the app uses every available question in random order. Add more data to generate a full exam.</p></div></div><button onClick={start} className="mt-8 flex w-full items-center justify-center gap-3 rounded-full bg-ai py-4 text-sm font-medium text-white hover:bg-[#0757d9]">Start exam <ArrowRight size={17}/></button></div></div></section>;
}
function Stat({icon,value,label}:{icon:React.ReactNode;value:string;label:string}) { return <div className="flex flex-col items-center gap-1 text-ai dark:text-[#60a5fa]"><span className="hidden sm:block">{icon}</span><b className="font-mono text-2xl font-normal text-sumi dark:text-white">{value}</b><small className="text-[10px] text-black/40 dark:text-white/40">{label}</small></div> }
function Mode({selected,onClick,title,text}:{selected:boolean;onClick:()=>void;title:string;text:string}) { return <button onClick={onClick} className={`relative rounded-xl border p-4 text-left ${selected?"border-ai bg-ai/[.06]":"border-black/10 dark:border-white/10"}`}><div className="flex items-center gap-3"><span className={`grid size-5 place-items-center rounded-full border ${selected?"border-ai bg-ai text-white":"border-black/20 dark:border-white/20"}`}>{selected&&<Check size={12}/>}</span><div><b className="block text-sm">{title}</b><small className="text-black/45 dark:text-white/45">{text}</small></div></div></button> }
